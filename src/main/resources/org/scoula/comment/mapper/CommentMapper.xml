<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.scoula.comment.mapper.CommentMapper">

    <!-- 장소별 댓글 조회 -->
    <select id="findByPlaceId"
            resultType="org.scoula.comment.vo.CommentVO">

        SELECT
        c.id,
        c.place_id AS placeId,
        c.user_id AS userId,
        c.content,
        u.nickname,
        c.created_at AS createdAt,
        c.updated_at AS updatedAt

        FROM comments c

        JOIN users u
        ON c.user_id = u.id

        WHERE c.place_id = #{placeId}

        ORDER BY c.created_at DESC

    </select>


    <!-- 댓글 작성 -->
    <insert id="insertComment">

        INSERT INTO comments (
        place_id,
        user_id,
        content
        )
        VALUES (
        #{placeId},
        #{userId},
        #{content}
        )

    </insert>


    <!-- 댓글 삭제 -->
    <delete id="deleteComment">

        DELETE FROM comments

        WHERE id = #{commentId}
        AND user_id = #{userId}

    </delete>

</mapper>