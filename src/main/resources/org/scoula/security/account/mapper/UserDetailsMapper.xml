<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.scoula.security.account.mapper.UserDetailsMapper">

    <resultMap id="authMap" type="AuthVO">
        <result property="username" column="email" />
        <result property="auth" column="auth" />
    </resultMap>

    <resultMap id="memberMap"  type="org.scoula.security.account.domain.MemberVO">
        <id property="id" column="id" />
        <result property="username" column="email" />
        <result property="password" column="password" />
        <result property="email" column="email" />
        <result property="nickname" column="nickname" />
        <result property="provider" column="provider" />
        <result property="providerId" column="provider_id" />
        <result property="regDate" column="created_at" />
        <result property="updateDate" column="updated_at" />
        <collection property="authList" resultMap="authMap" />
    </resultMap>

    <select id="get" resultMap="memberMap">
        SELECT
            id,
            email,
            password,
            nickname,
            provider,
            provider_id,
            created_at,
            updated_at,
            'ROLE_MEMBER' AS auth
        FROM users
        WHERE email = #{email}
          AND provider = 'LOCAL'
    </select>

    <select id="getById" resultMap="memberMap">
        SELECT id, email, password, nickname, provider, provider_id,
               created_at, updated_at, 'ROLE_MEMBER' AS auth
        FROM users
        WHERE id = #{id}
    </select>
</mapper>
